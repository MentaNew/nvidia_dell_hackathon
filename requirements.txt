# Tested 2026-09 on Python 3.14 (laptop) — target GB10 is Linux aarch64, python>=3.11
fastapi
uvicorn
python-multipart
openai
pymongo
pillow
imageio-ffmpeg   # bundles a static ffmpeg (x86_64 + aarch64 wheels) for recorded-replay frame sampling; system ffmpeg is used if present
